# CB-PAPER-PERFORMANCE-EVAL — pre-commit clarifications

**Filed:** 2026-05-02
**Target eval-execution date:** 2026-05-07 (per WORKSPACE pre-commitment)

Three open items the eval session must resolve before running. Each is
currently underdetermined by the pre-committed criteria; each can affect
a borderline verdict; each requires explicit choice to prevent post-hoc
reasoning at eval time.

## 1. Timestamp field for the eval-window filter

Two natural choices:

- **`placed_at`** — bets generated by post-Apr-17 paper-mode strategy
  (strategy-evaluation framing)
- **`ts`** — settlements landing in post-Apr-17 window
  (operational-results framing)

The 2026-05-02 preview-scan delta on the current corpus is 24 RESOLVED
events (1,691 with `placed_at` filter vs 1,715 with `ts` filter), too small
to flip the present preview verdict but methodologically load-bearing for
any future borderline run.

Eval session must pick one and document the rationale.

## 2. Drawdown criterion behavior when cumulative gross P&L ≤ 0

Pre-committed criterion: "drawdown ≤ 30% of cumulative gross profit."
Denominator undefined when cumulative ≤ 0 (the present preview case:
dd $72.81 vs cumulative ~-$14).

Two natural rules:

- **a) auto-FAIL** the drawdown criterion when cumulative ≤ 0
  (interprets a strategy that hasn't broken even as having unbounded
  drawdown ratio)
- **b) auto-N/A** — drawdown criterion does not gate the verdict when
  cumulative ≤ 0; verdict rests on the other criteria alone
  (interprets drawdown as a profit-protection metric, only meaningful
  when there are profits to protect)

Different reasonable people would pick different defaults. Pick before
eval-execution.

## 3. CB-METRICS-BLINDSPOT-OPEN-BOOK-WALLETS additive gates

Already specified in the WORKSPACE addendum to CB-PAPER-PERFORMANCE-EVAL.
Listed here for completeness: eval session must apply these gates
(source-wallet realized_ratio, MTM-inclusive PnL, per-wallet contribution
audit) before declaring a verdict. Any verdict that does not apply them
is conditional and not durable.

## Source

2026-05-02 preview-scan of `/opt/copybot/data/sports_signals.jsonl`
(snapshot frozen at `/tmp/sports_signals_b1.jsonl`, n=42,353 events;
eval-scope subset 1,691-1,715 RESOLVED depending on choice in item 1).
Preview verdict: INCONCLUSIVE on both filter choices.

Schema reference for the source file: `memory/semantic/sports_signals_schema.md`.
